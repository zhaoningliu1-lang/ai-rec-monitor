import MethodologyView from "@/components/views/MethodologyView";

export const metadata = {
  title: "Methodology — How Avanti Measures AI Visibility",
  description:
    "Query construction, engines and versions, mention detection, score definitions, and sampling honesty — every number in an Avanti report, explained.",
};

export default function MethodologyPage() {
  return <MethodologyView lang="en" />;
}
