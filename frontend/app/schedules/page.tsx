import SchedulesClient from "./SchedulesClient";

export const metadata = {
  title: "Auto Monitor — Avanti GEO",
  description: "Automated recurring AI visibility scans.",
};

export default function SchedulesPage() {
  return <SchedulesClient lang="en" />;
}
