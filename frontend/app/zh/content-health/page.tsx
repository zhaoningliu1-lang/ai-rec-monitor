import ContentHealthPage from "@/app/content-health/page";

// Chinese mirror — the ContentHealthPage is already bilingual-ready.
// For now, re-export the same client component.
export default ContentHealthPage;
